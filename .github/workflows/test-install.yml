name: test install all

on:
  - push
  - pull_request

jobs:
  run-program:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout this repo
      uses: actions/checkout@v3
    
    - name: Run the setup
      run: |
        sudo ./warpi_setup.sh
        mkdir /media/usb/
        touch /media/usb/warpi.log
        
        
    - name: Run the python
      run: |
        python warpigui.py
